# Halt — 2026-07-10 (macOS deps-card field report → issues #58/#59 + restart-instructions branch)

## ▶ Resume this session
```bash
cd /home/alisher/basecamp/modules/receiver-basecamp && claude --resume e3458885-06b5-4a43-a4c3-fba41bd87af5
```
`/home/alisher/basecamp/modules/receiver-basecamp` · session `e3458885-06b5-4a43-a4c3-fba41bd87af5`.
Fallback: `cd /home/alisher/basecamp/modules/receiver-basecamp && claude --continue`.

## Where we stopped
Handled a macOS field report (Alisher's photos, `~/Downloads/IMG_7356.HEIC` = the deps card, `IMG_7357.HEIC`
= About-This-Mac: MacBook Pro 16" 2021, **M1 Pro, macOS Tahoe 26.2**). Two problems: (1) `brew install`
auto-updated Homebrew first → needed a 2nd run; (2) the #57 deps card's `launchctl setenv RECEIVER_*_BIN`
commands ran clean but Re-check + relaunch still showed "installed — not visible." Filed **#58** (the bug)
and **#59** (brew friction), commented the field-verify result on **#56**. Then Alisher asked to fix the
README `$(which privoxy)` empty-path bug **and** put restart guidance in the card panel. **Working-copy
edits are DONE but NOT committed** — awaiting go-ahead to commit/PR.

## Current state
- Branch: **fix/mac-restart-instructions** (checked out from `origin/main`)
- Base commit: `3acd6cf` docs: README → v0.2.0.5 (#57 situation-aware card + darwin/mac release)
- **Uncommitted working-tree changes (2 files):**
  - `README.md` — added a "How to fully restart" 3-step block (⌘Q not window-close → confirm no helper
    survived via `pkill -f LogosBasecamp` / Kill Tor Listeners → reopen + Re-check), links #58.
  - `src/qml/Main.qml:451` — expanded the card's relaunch note to actionable (⌘Q, not window-close;
    Activity Monitor "Logos" Force-Quit fallback).
- Build status: **qmllint grammar-clean (0 hard parse errors)**; card render is **unverified** (needs a
  mac `.lgx` rebuild — inherently wetware). README markdown reviewed.
- Open review: none. Issues live: #58 (bug, macos, wetware-required), #59 (macos), #56 (verify tracker, commented).

## ⚠️ Critical context — the local checkout was STALE
`~/basecamp/modules/receiver-basecamp` was on `1383fa3`, **~6 commits behind `origin/main` (`3acd6cf`,
v0.2.0.5)**. Consequences already reconciled this session:
- The `$(which privoxy)` fix Alisher asked for was **already upstream** in `dd1d75f` ("explicit launchctl;
  privoxy in sbin") — my first README edit (on the stale base) was **discarded**, not committed. Don't redo it.
- The #57 card is **already shipped** (`0851856`, v0.2.0.5) — it IS the card in the photo. Earlier chat claim
  "card not in repo / host-side" was WRONG (stale tree). The card lives in `src/qml/Main.qml` + backend
  `depsJson`/`checkDeps`. Local `main` still sits at `1383fa3` — left as-is; work is on the new branch.

## Next steps (in order)
1. **Get Alisher's go-ahead**, then commit the branch (README + card) — ask before push. Suggested msg:
   "docs+ui(#58): macOS 'how to fully restart' — README steps + actionable card relaunch note (⌘Q, orphan check)".
2. **Verify the mac process name** for the `pkill` line — my `pkill -f LogosBasecamp` is a guess from Linux;
   one `ps ax | grep -i logos` after ⌘Q on the Mac confirms it. Note it on #58 so the mac session captures it.
3. Card change only reaches users via a **mac `.lgx` rebuild (v0.2.0.6)** — rides the #58/#56 mac session.
4. Optional: fold into #58 the two sharper points from the chat — (a) #56's "clears with **no relaunch**"
   acceptance wording is wrong for the launchctl branch (Re-check runs in-process, env frozen at launch);
   (b) fix idea: make Re-check shell out to `launchctl getenv RECEIVER_*_BIN` so it clears without relaunch.
5. #59: propose `HOMEBREW_NO_AUTO_UPDATE=1 brew install …` in the card's brew installCmd + README (headless).

## Blockers
- **Commit/push** needs Alisher's explicit go-ahead (not yet given).
- **Mac verification** (wetware): exact host process name for `pkill`; card render/wrapping; whether #58's
  launchctl-doesn't-take-effect is sandbox (hyp.1) vs env-not-inherited (hyp.2) vs stale-host #51 (hyp.3) —
  isolate with `launchctl getenv RECEIVER_TOR_BIN` + `ps eww <ui-host pid>` post-relaunch on the Mac.

## Context that's hard to re-derive
- HEIC photos wouldn't convert with heif-convert/ffmpeg ("too many auxiliary image references"); **pillow_heif
  worked** — converted PNGs in scratchpad (`/tmp/claude-1000/.../e3458885-.../scratchpad/IMG_7356.png`,`IMG_7357.png`).
- Mechanism confirmed for #58: `launchctl setenv` seeds only FUTURE launches; a running process's env is
  frozen at exec, and Re-check calls `checkDeps()` **in-process** → cannot see a post-launch setenv without a
  real relaunch. And `receiver_relay/src/receiver_relay_plugin.cpp:34` — `dieWithParent` is a **darwin no-op**
  (PR_SET_PDEATHSIG is Linux-only) → helper hosts orphan on quit (ties #51), so "fully quit" is non-trivial.
- The released module also lives at `~/basecamp/logos-modules-release/submodules/receiver-basecamp` (same
  origin, was ahead at `3acd6cf`); installed build at `~/.local/share/Logos/LogosBasecamp/plugins/receiver_ui/`.
