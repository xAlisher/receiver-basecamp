# Halt — 2026-06-12

Two efforts this session, both at clean stopping points. The only live thread is **radio-basecamp /
Felipe** (waiting on his reply). The receiver mac effort is concluded (mac blocked → demo on Linux).

## Where we stopped

**1. radio-basecamp — helped Felipe (external user) install radio_module.** He hit two walls: the 268
AppImage needs glibc ≥2.38 (he worked around it with a distrobox-24.04 / glibc-2.39 container), and
`delivery_module` isn't bundled in any AppImage or catalog. Resolved: built a prebuilt **delivery v0.1.1
linux-amd64 lgx**, committed it to `dist/` + attached to the `v0.1.0` release, and rewrote the README's
install section with all paths. Drafted a reply message to Felipe (in chat, not yet confirmed sent).

**2. receiver-basecamp — mac demo workaround, DISPROVEN.** Built a `type:core` relay
(`receiver_relay`) + pure-QML `receiver_ui` to dodge the ui-host onEvent bug by receiving in logos_host.
Definitive result: cross-module events don't dispatch in logos_host **either** (49 delivery emits → 0
relay callbacks) — it's the QRO-replica IPC boundary, not the host. mac is blocked until the platform
fix ships. Decision (user): **demo on Linux** (proven working on 268). Retro done, skills extracted,
issue #4 updated.

## Current state

- **radio-basecamp**: branch `main`, last commit `ba867c4` "docs+dist: ship prebuilt delivery_module
  v0.1.1 lgx + document all install paths". Pushed. delivery lgx on the `v0.1.0` release. Build: lgx
  built clean (exit 0, 19MB, linux-amd64, self-contained). README install section verified live.
- **receiver-basecamp**: branch `feat/mac-core-relay`, last commit `9d11819` "retro(mac): document the
  event-IPC wall + build/load quirks". Pushed. Relay + pure-QML ui build on darwin-arm64 but cannot
  receive (platform bug). main is untouched/clean.
- **basecamp-skills**: branch `opus-4.8/triggers-and-preview`, commit `fbc3635` — 3 darwin skills
  extracted + indexed, pushed.
- Open review: none.

## Next steps (in order)

1. **Wait for Felipe's reply** (radio install). If he hits more walls, debug from his logs. Reply
   message is drafted in chat — confirm with Alisher it was sent.
2. **(Optional, offered to Alisher) add `delivery_module` to the catalog** (`logos-repo.json` in
   xAlisher/logos-basecamp-modules) so `lgpd install delivery` works — removes the manual step for the
   next user. Bigger change; do as a follow-up only if Alisher says yes.
3. **receiver mac**: nothing to do until the platform/cpp-sdk event-dispatch fix ships in a new
   AppImage. When it does: revert the relay, go back to the direct ui_qml event consumer on main, retest
   on mac. `feat/mac-core-relay` is a throwaway branch.
4. **receiver-basecamp**: did NOT clear `docs/retro-log.md` (still holds 2026-06-11 + 2026-06-12
   entries). Optional Auditor pass (`/codex:rescue` Senty review of the retro) was offered, not run.

## Blockers

- **receiver mac discovery**: blocked on the platform — cross-module SDK events never dispatch on macOS
  (QRO-replica IPC / CFRunLoop, QTBUG-39488). No API workaround (delivery has no poll method). Needs
  upstream fix in an AppImage. Tracked: receiver-basecamp#4, cpp-sdk #68/#79.
- Felipe thread: waiting on his next message (not a blocker on us).

## Context that's hard to re-derive

- **delivery pin discipline**: radio_module pins delivery **v0.1.1 = rev `0c346c0c`** (metadata version
  reads `1.1.0`). Felipe referenced `d872f77c` (from delivery main) — that's WRONG for radio; the API
  drift breaks radio_module with `"Invalid response"`. receiver pins delivery `main` ONLY because v0.1.1
  fails to build on darwin/arm64 — a mac-only reason.
- **mac build quirk**: C++ modules on mac MUST build `.#lgx-portable` (variant `darwin-arm64`, bundled
  dylibs). The dev `.#lgx` (`darwin-arm64-dev`) silently won't load — wasted a cycle on this. Pure-QML
  modules load fine from either. (skill: `darwin-lgx-portable-required`.)
- **mac core modules load on-demand** — installing a core module alone shows zero activity until a
  consumer getClients it / a ui dep pulls it. (skill: `darwin-core-module-on-demand-load`.)
- **What works on mac**: zerokit builds on darwin-arm64; `getClient` + invokeRemoteMethod (request/reply)
  work; only the async event path is dead. receiver UI loads, goes green — just no stations.
- The mac runs Diana's `LogosBasecamp.app` (tailscale `mac` ssh host). Profile:
  `~/Library/Application Support/Logos/LogosBasecamp/{modules,plugins}`. receiver_ui currently disabled
  copy at `/tmp/receiver_ui_disabled` on the mac (the old QRO version, moved aside during testing).
- Diagnostic qDebug logs are still in `receiver_relay`'s onEvent lambda + ingestAnnounce (committed
  `f9af77f`) — remove them when reverting the relay.
